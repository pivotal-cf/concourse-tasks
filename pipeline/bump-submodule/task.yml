---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang

inputs:
  - name: release-repo
  - name: submodule-repo

outputs:
  - name: bumped-release-repo

run:
  path: release-repo/ci/acceptance/scripts/bump_submodule.sh

params:
  SUBMODULE_PATH:
  GIT_USER_NAME:
  GIT_USER_EMAIL:
